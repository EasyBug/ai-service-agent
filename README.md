# AI Service Agent

智能客服系统 - 基于 LangGraph、LlamaIndex 和 Next.js 的智能问答与订单管理系统

## 项目简介

这是一个完整的智能客服系统，包含以下功能：

- 🤖 **智能问答**：基于 RAG（检索增强生成）的知识库问答
- 📦 **订单查询**：订单信息查询和管理
- 📧 **邮件通知**：订单状态变更邮件通知（n8n 集成）
- 🔐 **用户认证**：JWT 认证和基于角色的访问控制（RBAC）
- 📚 **知识库管理**：支持文件上传、索引更新和内容管理

## 技术栈

### 后端
- **FastAPI** - Web 框架
- **LangGraph** - Agent 工作流编排
- **LlamaIndex** - RAG 知识库检索
- **PostgreSQL + pgvector** - 数据存储和向量数据库
- **Redis** - LangGraph 状态存储
- **Google Gemini** - LLM 和 Embedding 模型
- **n8n** - 自动化工作流（邮件发送）

### 前端
- **Next.js 15** (App Router) - React 框架
- **TypeScript** - 类型安全
- **TailwindCSS** - 样式框架
- **Shadcn/UI** - UI 组件库
- **Zustand** - 状态管理
- **Axios** - HTTP 客户端

## 项目结构

```
ai-service-agent/
├── back/                 # 后端代码
│   ├── app/             # 应用主目录
│   │   ├── agent/       # LangGraph Agents
│   │   ├── clients/     # 外部服务客户端
│   │   ├── db/          # 数据库模型和 CRUD
│   │   ├── rag/         # RAG 相关功能
│   │   ├── router/      # API 路由
│   │   └── utils/       # 工具函数
│   ├── data/            # 知识库文档目录
│   └── requirements.txt # Python 依赖
├── frontend/            # 前端代码
│   ├── app/             # Next.js App Router 页面
│   ├── components/      # React 组件
│   └── lib/             # 工具库和 API 客户端
└── README.md            # 项目说明文档
```

## 快速开始

### 后端设置

1. 安装依赖：
```bash
cd back
pip install -r requirements.txt
```

2. 配置环境变量（复制 `.env.example` 为 `.env` 并填写）：
```bash
cp .env.example .env
```

3. 初始化数据库：
```bash
python init_db.py
python seed_users.py
python seed_orders.py
```

4. 启动服务：
```bash
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

### 前端设置

1. 安装依赖：
```bash
cd frontend
npm install
```

2. 配置环境变量（可选）：
```bash
cp .env.example .env.local
```

3. 启动开发服务器：
```bash
npm run dev
```

## 功能说明

### 用户角色

- **admin**：管理员，拥有所有权限
- **test**：测试用户，可以访问知识库
- **user**：普通用户，只能使用智能问答和订单查询

### 测试账号

- 管理员：`admin@example.com` / `Admin@2024!Secure`
- 测试用户：`test@example.com` / `Test@2024!Secure`
- 普通用户：`user@example.com` / `User@2024!Secure`

## 文档

详细文档请参考：

- [后端 README](back/README.md)
- [前端 README](frontend/README.md)
- [用户设置指南](back/USER_SETUP_GUIDE.md)
- [知识库测试数据指南](back/data/RAG_TEST_DATA_GUIDE.md)

## 许可证

MIT License

